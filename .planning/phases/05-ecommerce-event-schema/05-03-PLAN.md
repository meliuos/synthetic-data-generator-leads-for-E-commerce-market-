---
phase: 5
plan: 3
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - docs/schema-v1.1.md
  - README.md
autonomous: true

must_haves:
  truths:
    - "docs/schema-v1.1.md documents all 8 new e-commerce columns with their ClickHouse types and JSON source-shape"
    - "docs/schema-v1.1.md documents both sibling tables (analytics.purchase_items, analytics.orders) including their engines and query patterns"
    - "docs/schema-v1.1.md contains a dedicated paragraph explaining why the roadmap's projection language is implemented as secondary materialized views, citing RESEARCH.md §4 and §7.3"
    - "docs/schema-v1.1.md documents the GA4 alias view analytics.click_events_ga4 with an alias-mapping table"
    - "docs/schema-v1.1.md tells the reader how to apply the migration (make schema-v11) and how to verify it (make smoke-test-v11)"
    - "README.md contains a new link pointing to docs/schema-v1.1.md and mentions the make schema-v11 target"
    - "No unrelated README sections are restructured or deleted"
  artifacts:
    - path: "docs/schema-v1.1.md"
      provides: "Developer reference for v1.1 additive e-commerce schema extension"
      contains: "analytics.purchase_items"
      contains_alt: "secondary materialized view"
    - path: "README.md"
      provides: "Pointer from the project root README into docs/schema-v1.1.md"
      contains: "docs/schema-v1.1.md"
  key_links:
    - from: "README.md"
      to: "docs/schema-v1.1.md"
      via: "Markdown link in Schema / Documentation section"
      pattern: "\\[.*\\]\\(docs/schema-v1\\.1\\.md\\)"
    - from: "docs/schema-v1.1.md"
      to: "infra/clickhouse/sql/002_ecommerce_schema.sql"
      via: "Link in the migration section"
      pattern: "002_ecommerce_schema\\.sql"
    - from: "docs/schema-v1.1.md"
      to: ".planning/phases/05-ecommerce-event-schema/05-RESEARCH.md"
      via: "Citation in the substitution-rationale paragraph"
      pattern: "05-RESEARCH\\.md"
---

<objective>
Author `docs/schema-v1.1.md` — a concise developer reference describing the v1.1 additive schema extension (8 new columns, 2 sibling tables, GA4 alias view, idempotent migration) — and add a pointer from `README.md`. The doc must include a mandatory substitution-rationale paragraph explaining why the roadmap's "projection" language became sibling materialized views in implementation, with a citation to `.planning/phases/05-ecommerce-event-schema/05-RESEARCH.md` §4 and §7.3.

Purpose: Close the documentation gap so future contributors (and future-you) understand the deliberate deviation from roadmap wording without having to re-derive the research.
Output: One new docs file and a surgical README edit.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-ecommerce-event-schema/05-RESEARCH.md
@.planning/phases/05-ecommerce-event-schema/05-01-PLAN.md
@README.md
@infra/clickhouse/sql/001_events_schema.sql

# The doc is a READER reference — not a design doc, not a tutorial.
# Keep it under 250 lines. Link to RESEARCH.md for depth; summarize here.
# Assume the reader knows ClickHouse basics (MergeTree, materialized views).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author docs/schema-v1.1.md</name>
  <files>docs/schema-v1.1.md</files>
  <action>
Create a new file `docs/schema-v1.1.md`. The `docs/` directory may not exist yet — create it alongside the file write if needed.

Structure (8 sections, in this order, using `##` for section headers and `###` for subsections):

**1. Title + one-paragraph intro** (`# Schema v1.1 — E-commerce Events`)
- What this extension adds (8 Nullable columns + 2 sibling tables + GA4 view).
- What it deliberately does NOT do (modify v1.0 schema, rebuild the base table).
- Traceability: implements SCHEMA-01, SCHEMA-02, SCHEMA-03 from REQUIREMENTS.md.

**2. New columns on `analytics.click_events`** — one table, 8 rows, 4 columns: `Column | Type | Source shape | Example`. Types exactly per RESEARCH.md §3:

| Column | Type | Source shape | Example |
|--------|------|--------------|---------|
| `product_id` | `Nullable(String)` | flat or `properties.product_id` | `"SKU-100"` |
| `category` | `Nullable(String)` | flat or `properties.category` | `"electronics/headphones"` |
| `price` | `Nullable(Decimal(18,2))` | flat or `properties.price` | `99.99` |
| `quantity` | `Nullable(UInt32)` | flat or `properties.quantity` | `3` |
| `order_id` | `Nullable(String)` | flat or `properties.order_id` | `"ord-12345"` |
| `cart_value` | `Nullable(Decimal(18,2))` | flat or `properties.cart_value` | `139.49` |
| `search_query` | `Nullable(String)` | `properties.query` (or GA4 `search_term` fallback) | `"wireless"` |
| `results_count` | `Nullable(UInt32)` | flat or `properties.results_count` | `42` |

One-paragraph note: "All columns are `Nullable`. v1.0 events (click, scroll, pageview, session_start) continue inserting unchanged with NULLs in these fields. The materialized view extracts from both flat top-level JSON keys and nested `properties` JSON, matching the v1.0 fallback pattern."

**3. Sibling tables (NOT projections)** — this is where the substitution rationale lives. Include the mandatory paragraph verbatim below:

> **Why sibling materialized views instead of projections?**
> The v1.1 roadmap originally described a `products[]` ARRAY JOIN projection and a `ReplacingMergeTree(event_time)` projection keyed on `order_id`. Pre-planning research proved both are technically impossible in ClickHouse:
> - Projections cannot use `ARRAY JOIN` — see `.planning/phases/05-ecommerce-event-schema/05-RESEARCH.md` §4 and GitHub [ClickHouse #98953](https://github.com/ClickHouse/ClickHouse/issues/98953).
> - Projections inherit the base table's storage engine; they cannot declare `ENGINE = ReplacingMergeTree(...)` — see `.planning/phases/05-ecommerce-event-schema/05-RESEARCH.md` §7.3 and GitHub [ClickHouse #24778](https://github.com/ClickHouse/ClickHouse/issues/24778), [#46968](https://github.com/ClickHouse/ClickHouse/issues/46968).
>
> The functional outcomes the roadmap intended (per-line-item queries, server-side purchase dedup) are preserved by writing two secondary materialized views that read from the same `analytics.events_queue` Kafka source and fan out into dedicated sibling tables described below.

Then describe each sibling table with a small subsection:

- **`analytics.purchase_items`** (`MergeTree`, ordered by `(order_id, product_id, event_time)`): one row per line item per purchase event. Populated by the `analytics.purchase_items_mv` materialized view which `arrayJoin`s `JSONExtractArrayRaw(properties, 'products')`. Columns: event_id, event_time, order_id, anonymous_user_id, session_id, product_id, sku, name, category, price, quantity, position, currency. Use for "how many of product X sold?", "category mix per order", per-line SKU queries.

- **`analytics.orders`** (`ReplacingMergeTree(event_time)`, keyed on `order_id`): one deduplicated row per order. Populated by the `analytics.orders_mv` materialized view which writes one row per `purchase` / `Order Completed` event. Columns: order_id, event_time, anonymous_user_id, session_id, total, revenue, tax, shipping, discount, currency, coupon, products_json. **Query tip:** use `SELECT ... FROM analytics.orders FINAL` or `argMax(col, event_time)` to force query-time dedup — background merge is asynchronous.

**4. Main MV update** — one short paragraph. `analytics.events_mv` is atomically replaced via `CREATE OR REPLACE MATERIALIZED VIEW` (no ingest gap; Kafka consumer offsets on `analytics.events_queue` are preserved). The SELECT adds the 8 new columns using the same flat-then-properties fallback pattern as v1.0. Historical rows already in `click_events` will have NULL in the new columns — this is expected (see RESEARCH.md §8.5).

**5. GA4 alias view `analytics.click_events_ga4`** — one paragraph explaining this is a zero-storage, read-time-only remapping view. Alias table:

| v1.1 column | GA4 alias |
|-------------|-----------|
| `product_id` | `item_id` |
| `category` | `item_category` |
| `order_id` | `transaction_id` |
| `search_query` | `search_term` |

Note: GA4's `items[]` array is not exposed on this view — consumers that need per-item data should query `analytics.purchase_items` directly.

**6. Running the migration**

```bash
make schema-v11
```

- Idempotent: safe to re-run (uses `ADD COLUMN IF NOT EXISTS`, `CREATE TABLE IF NOT EXISTS`, `CREATE MATERIALIZED VIEW IF NOT EXISTS`, `CREATE OR REPLACE`).
- Does NOT drop or rebuild existing v1.0 objects.
- Does NOT modify `infra/clickhouse/sql/001_events_schema.sql`.
- Underlying SQL: [`infra/clickhouse/sql/002_ecommerce_schema.sql`](../infra/clickhouse/sql/002_ecommerce_schema.sql).

**7. Verifying the migration**

```bash
make smoke-test-v11
```

Drives 4 events (flat, nested, purchase with line items, duplicate purchase) through the real pipeline and asserts: 8 new columns populate, per-line-item fan-out works, `order_id` dedup works, GA4 aliases exist. Script: [`scripts/smoke-test-v11.sh`](../scripts/smoke-test-v11.sh).

**8. Related references**
- Roadmap — [`.planning/ROADMAP.md`](../.planning/ROADMAP.md) (Phase 5 entry)
- Requirements — [`.planning/REQUIREMENTS.md`](../.planning/REQUIREMENTS.md) (SCHEMA-01..03)
- Research (full rationale, pitfalls, ClickHouse citations) — [`.planning/phases/05-ecommerce-event-schema/05-RESEARCH.md`](../.planning/phases/05-ecommerce-event-schema/05-RESEARCH.md)
- Migration SQL — [`infra/clickhouse/sql/002_ecommerce_schema.sql`](../infra/clickhouse/sql/002_ecommerce_schema.sql)

Writing conventions:
- Fenced code blocks have language tags (`bash`, `sql`).
- Keep the total length under 250 lines. This is a reference, not a book.
- All internal links relative to the file's own location (the file lives in `docs/`, so repo-root files resolve via `../`).
  </action>
  <verify>
# Directory and file exist with non-zero size
test -d docs
test -s docs/schema-v1.1.md

# Requirement traceability
grep -q "SCHEMA-01" docs/schema-v1.1.md
grep -q "SCHEMA-02" docs/schema-v1.1.md
grep -q "SCHEMA-03" docs/schema-v1.1.md

# All 8 new columns documented by name
for col in product_id category price quantity order_id cart_value search_query results_count; do
  grep -q "$col" docs/schema-v1.1.md || { echo "MISSING column: $col"; exit 1; }
done

# Sibling tables and their engines
grep -q "analytics.purchase_items" docs/schema-v1.1.md
grep -q "analytics.orders" docs/schema-v1.1.md
grep -q "ReplacingMergeTree" docs/schema-v1.1.md

# GA4 alias view and alias names
grep -q "click_events_ga4" docs/schema-v1.1.md
grep -q "item_id" docs/schema-v1.1.md
grep -q "transaction_id" docs/schema-v1.1.md
grep -q "search_term" docs/schema-v1.1.md

# Migration + verification entry points
grep -q "make schema-v11" docs/schema-v1.1.md
grep -q "make smoke-test-v11" docs/schema-v1.1.md

# Substitution rationale paragraph (mandatory)
grep -q "secondary materialized view" docs/schema-v1.1.md
grep -q "projection" docs/schema-v1.1.md
grep -q "05-RESEARCH.md" docs/schema-v1.1.md

# Length sanity — under 250 lines
test $(wc -l < docs/schema-v1.1.md) -lt 250
  </verify>
  <done>
`docs/schema-v1.1.md` exists, documents all 8 new columns with types and source-shape, both sibling tables with engines and query patterns, GA4 alias view with the full alias-mapping table, `make schema-v11` and `make smoke-test-v11` entry points. Contains the mandatory substitution-rationale paragraph citing RESEARCH.md §4 and §7.3 with GitHub issue links. Total length under 250 lines.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add README.md pointer to the new docs file</name>
  <files>README.md</files>
  <action>
Open `README.md` and insert a link to `docs/schema-v1.1.md`. Use the first matching strategy below (read the existing README first — do not guess):

**Strategy A (preferred)** — if the README already has a "Schema", "Architecture", "Documentation", or similar section, append one bullet:

```markdown
- [Schema v1.1 (e-commerce events)](docs/schema-v1.1.md) — 8 new Nullable columns on `analytics.click_events`, per-line-item `purchase_items` table, server-side `orders` dedup, GA4 alias view. Apply via `make schema-v11`; verify via `make smoke-test-v11`.
```

**Strategy B (fallback)** — if no suitable section exists, insert a new `## Schema Reference` section BELOW the project description/intro and ABOVE the first operational section (Quick Start / Installation / Makefile targets). Section body:

```markdown
## Schema Reference

- [v1.1 — E-commerce events](docs/schema-v1.1.md) — 8 new columns on `analytics.click_events`, two sibling tables (`purchase_items`, `orders`), GA4 alias view. Apply via `make schema-v11`; verify via `make smoke-test-v11`.
```

**Strategy C** — if the README has a Makefile / make targets listing section, ALSO add an entry for `make schema-v11` (and `make smoke-test-v11` if not already noted by plan 05-02) alongside the existing `make schema` / `make smoke-test` entries, with a one-line description.

Hard rules:
- Surgical edit only. Do NOT restructure, rewrite, or reorder unrelated sections.
- Do NOT duplicate content from `docs/schema-v1.1.md` into the README. The README points; the docs file explains.
- If a "Schema v1.1" link already exists (from a prior partial run), update it — do not add a duplicate.
  </action>
  <verify>
# README references the new doc
grep -q "docs/schema-v1.1.md" README.md

# README references at least one of the new make targets in context of v1.1
grep -qE "schema-v11|smoke-test-v11" README.md

# README was not trivially blown away
test $(wc -l < README.md) -ge 5
  </verify>
  <done>
`README.md` contains a Markdown link to `docs/schema-v1.1.md` and mentions at least one of the new Makefile targets (`schema-v11` or `smoke-test-v11`). No unrelated sections were modified.
  </done>
</task>

</tasks>

<verification>
Post-plan checks:

1. **Docs file exists and is well-formed:**
   ```bash
   test -s docs/schema-v1.1.md
   wc -l docs/schema-v1.1.md     # should be roughly 80–250 lines
   ```

2. **All required topics present in the docs file:**
   ```bash
   for topic in "SCHEMA-01" "SCHEMA-02" "SCHEMA-03" \
                "purchase_items" "orders" "ReplacingMergeTree" \
                "click_events_ga4" "item_id" "transaction_id" "search_term" \
                "make schema-v11" "make smoke-test-v11" \
                "secondary materialized view" "05-RESEARCH.md"; do
     grep -q "$topic" docs/schema-v1.1.md || { echo "MISSING: $topic"; exit 1; }
   done
   echo "All required topics present"
   ```

3. **README pointer in place, make targets mentioned:**
   ```bash
   grep -q "docs/schema-v1.1.md" README.md
   grep -qE "schema-v11|smoke-test-v11" README.md
   ```

4. **Substitution rationale paragraph is present and cites research:**
   ```bash
   grep -A5 "secondary materialized view" docs/schema-v1.1.md | grep -q "05-RESEARCH.md"
   ```

5. **Internal links resolve (paths exist):**
   ```bash
   # The doc uses ../ relative links; check that referenced files actually exist
   test -f infra/clickhouse/sql/002_ecommerce_schema.sql || echo "NOTE: 002_ecommerce_schema.sql not yet created (expected if 05-01 has not run)"
   test -f .planning/phases/05-ecommerce-event-schema/05-RESEARCH.md
   test -f .planning/ROADMAP.md
   test -f .planning/REQUIREMENTS.md
   ```
</verification>

<success_criteria>
- `docs/schema-v1.1.md` exists and passes all topic-presence checks
- Substitution-rationale paragraph is present and cites RESEARCH.md §4 and §7.3 with GitHub issue links
- All 8 new columns are listed with their exact ClickHouse types
- Both sibling tables are described with their engines and query patterns
- GA4 alias table lists all 4 renames (product_id → item_id, category → item_category, order_id → transaction_id, search_query → search_term)
- `README.md` links to the new doc and mentions at least one of `make schema-v11` / `make smoke-test-v11`
- No unrelated README sections were modified
- Doc length under 250 lines
</success_criteria>

<must_haves>
- **Contract documentation**: every developer-visible artifact from Plans 05-01 and 05-02 (8 new columns, 2 sibling tables, GA4 view, make schema-v11, make smoke-test-v11) is findable from `docs/schema-v1.1.md` within ~30 seconds of skimming.
- **Substitution rationale preserved**: the "projection → secondary MV" substitution is permanently documented so a future contributor who reads the roadmap, sees "projection," then checks the actual schema, understands the intentional deviation without having to re-derive the ClickHouse limitations.
- **Research citation**: the substitution paragraph links to `.planning/phases/05-ecommerce-event-schema/05-RESEARCH.md` §4 and §7.3 and includes GitHub issue references (#98953, #24778, #46968) so the primary-source evidence is two clicks away.
- **README discoverability**: project root README points to the v1.1 schema doc — new contributors can find it without prior knowledge of the `docs/` directory layout.
- **No duplication**: the README does NOT repeat the full column list or query patterns — that is the docs file's job. Minimizes drift risk when schema evolves.
- **Length discipline**: docs file under 250 lines total — this is a reference, not a textbook. Anything deeper lives in RESEARCH.md.
</must_haves>

<output>
After completion, create `.planning/phases/05-ecommerce-event-schema/05-03-SUMMARY.md` documenting:
- The 8 doc sections authored
- Where in README the pointer was placed (which strategy A/B/C was used)
- Confirmation that the substitution-rationale paragraph cites RESEARCH.md §4 and §7.3 plus all three GitHub issues
- Pattern established: per-milestone schema docs live at `docs/schema-v<MAJOR>.<MINOR>.md` and are always linked from the root README
</output>
