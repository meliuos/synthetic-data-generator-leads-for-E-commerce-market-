---
phase: 02-js-tracker-and-event-ingestion
plan: 03
type: execute
wave: 1
depends_on: ["02-01"]
files_modified: [package.json, src/tracker/consent.js, src/tracker/index.js, src/test-page.html]
autonomous: true

must_haves:
  truths:
    - "vanilla-cookieconsent v3.1.0 banner appears on page load"
    - "No events are captured until user accepts cookie consent"
    - "Once consent is accepted, all subsequent events are captured and queued"
    - "Consent acceptance is persisted (localStorage) so banner does not reappear"
    - "Verified: reload page after consenting → banner does not appear, events continue to be captured"
  artifacts:
    - path: "src/tracker/consent.js"
      provides: "Cookie consent gate integration"
      exports: ["initConsentGate", "hasConsent"]
    - path: "package.json"
      provides: "vanilla-cookieconsent v3.1.0 dependency"
    - path: "src/tracker/index.js"
      provides: "Wrapped event capture with consent check"
      pattern: "if\\s*\\(.*hasConsent.*\\)|consentGate"
    - path: "src/test-page.html"
      provides: "Test page with tracker snippet and consent banner"
  key_links:
    - from: "Event capture functions (captureClick, captureScroll)"
      to: "hasConsent() check"
      via: "Wrap all event handlers with if (hasConsent())"
      pattern: "if.*hasConsent.*\\{.*capture"
    - from: "vanilla-cookieconsent banner"
      to: "localStorage 'cookie_consent_accepted'"
      via: "onConsent callback"
      pattern: "localStorage.*cookie_consent|onConsent.*"
---

<objective>
Integrate vanilla-cookieconsent v3.1.0 banner as a gate to event capture, ensuring no events are recorded until the user explicitly accepts cookies (GDPR compliance).

Purpose: Implement TRACK-06 requirement; gate all event capture to meet cookie consent regulation before events reach Redpanda.
Output: Functional cookie consent banner blocking all tracker events until accepted.
</objective>

<execution_context>
@/home/mootez/.claude/get-shit-done/workflows/execute-plan.md
@/home/mootez/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
Phase 1 complete. Tracker events are capturing and flowing through RudderStack to ClickHouse (Phase 2.01-02).

Requirement TRACK-06: "Cookie consent gate blocks all event capture until user accepts — GDPR compliant"

vanilla-cookieconsent v3.1.0 documentation: https://github.com/orestbida/cookieconsent
- Lightweight, no external dependencies
- Configuration via object passed to CookieConsent.run()
- onConsent callback fires when user accepts
- localStorage stores consent state

@/home/mootez/Desktop/pfa/.planning/phases/02-js-tracker-and-event-ingestion/02-01-SUMMARY.md (after 02-01)
@/home/mootez/Desktop/pfa/.planning/phases/02-js-tracker-and-event-ingestion/02-02-SUMMARY.md (after 02-02)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install vanilla-cookieconsent and create consent gate module</name>
  <files>package.json, src/tracker/consent.js</files>
  <action>
    1. Add to package.json:
       - "vanilla-cookieconsent": "^3.1.0"
       - npm install
       
    2. Create src/tracker/consent.js and export:
       - initConsentGate(onConsentCallback):
         * Load vanilla-cookieconsent
         * Configure banner with CookieConsent.run({
             categories: { necessary: {}, analytics: {} },
             onConsent: (consent) => { localStorage.setItem('cookie_consent_accepted', 'true'); onConsentCallback() },
             onFirstConsent: (consent) => { same as onConsent }
           })
         * Call CookieConsent.run() on page load
         
       - hasConsent(): boolean
         * Return localStorage.getItem('cookie_consent_accepted') === 'true'
         * If never set, return false (no consent yet)
         
    3. CSS styling (inline or separate):
       - Place banner at bottom of page
       - Style with neutral colors (gray/white), "Accept" button prominent
       - Make dismissible without accepting (users can close but events still don't fire)
       
    Simple implementation - ready for styling enhancement in later phase.
  </action>
  <verify>
    - [ ] vanilla-cookieconsent installed: npm list vanilla-cookieconsent
    - [ ] src/tracker/consent.js exports initConsentGate and hasConsent
    - [ ] hasConsent() returns false initially, true after localStorage is set
    - [ ] No errors importing and calling functions
  </verify>
  <done>vanilla-cookieconsent installed and consent gate module created.</done>
</task>

<task type="auto">
  <name>Task 2: Gate all tracker event capture with consent checks</name>
  <files>src/tracker/index.js, src/tracker/events.js</files>
  <action>
    1. Update src/tracker/events.js:
       - Each event function (captureClick, captureScroll, captureMousemove, capturePageView) should:
         * Check if (hasConsent()) at the start
         * If no consent: return early (do not queue event)
         * If consent: proceed with capture
         
    2. Alternative pattern - wrap at attachment level in src/tracker/index.js:
       - Instead of checking inside each function, wrap listeners:
         * const handler = (event) => { if (hasConsent()) captureClick(event) }
         * document.addEventListener('click', handler)
         
       - This keeps event functions pure and consent logic centralized
       
    3. Import consent module:
       - import { hasConsent, initConsentGate } from './consent'
         
    4. Call initConsentGate in tracker init:
       - initConsentGate(() => { console.log('Consent accepted'); flushEvents() })
       - This way first flush happens immediately after consent
  </action>
  <verify>
    - [ ] Click element before consent: event queue is empty
    - [ ] Accept consent in banner
    - [ ] Click element after consent: event queue has click event
    - [ ] Scroll after consent: event queue has scroll event
    - [ ] Verify: localStorage.getItem('cookie_consent_accepted') === 'true' after accepting
  </verify>
  <done>All event capture gated with consent check. No events recorded until acceptance.</done>
</task>

<task type="auto">
  <name>Task 3: Create test page with consent banner and verify gating behavior</name>
  <files>src/test-page.html</files>
  <action>
    1. Create src/test-page.html with:
       - Basic HTML page (title, headings, paragraph, buttons, scrollable content)
       - Script tag loading tracker: <script src="./tracker/index.js"></script>
       - Script tag loading consent: <script src="./tracker/consent.js"></script>
       - On page load: call initTracker() then initConsentGate()
       - Scroll content to make page >100vh height (test scroll events)
       - Include clickable elements (buttons, links) to test click events
       
    2. Manual testing:
       - Load page in browser
       - Verify vanilla-cookieconsent banner appears at bottom
       - Attempt to click on page: open DevTools console, check tracker event queue → should be empty
       - Click "Accept" button on banner
       - Click again on page: event queue should now contain click event
       - Reload page: banner should NOT reappear (consent persisted)
       - Click and scroll: events should be captured
       
    3. Verify localStorage:
       - After accepting: console.log(localStorage) → shows 'cookie_consent_accepted'
       - After closing browser tab and reopening: banner doesn't appear
  </action>
  <verify>
    - [ ] src/test-page.html loads without errors
    - [ ] vanilla-cookieconsent banner appears on load
    - [ ] Before consent: clicking/scrolling does not populate event queue
    - [ ] After consent: clicking/scrolling populates event queue
    - [ ] Page reload: banner does not reappear (consent persisted)
    - [ ] Console: no errors related to consent or tracker
  </verify>
  <done>Test page created. Consent gating verified working end-to-end.</done>
</task>

</tasks>

<verification>
Consent banner blocks all event capture until accepted. EventQueue remains empty until user approves cookies. After acceptance, all event types are captured and flowed through the pipeline.
</verification>

<success_criteria>
- [ ] vanilla-cookieconsent v3.1.0 installed
- [ ] initConsentGate() displays banner on page load
- [ ] hasConsent() returns false until banner is accepted
- [ ] All event capture functions check hasConsent() before queuing
- [ ] Test page demonstrates gating: no events before consent, events after
- [ ] localStorage persists consent across page reloads
- [ ] GDPR compliant: no data collected until explicit acceptance
- [ ] Ready for end-to-end validation with browser testing (Phase 2.04)
</success_criteria>

<output>
After completion, create `.planning/phases/02-js-tracker-and-event-ingestion/02-03-SUMMARY.md`
</output>
