---
phase: 02-js-tracker-and-event-ingestion
plan: 02
type: execute
wave: 1
depends_on: ["02-01"]
files_modified: [package.json, src/tracker/rudderstack.js, src/tracker/index.js, infra/rudderstack/workspaceConfig.json, .env.example, Makefile]
autonomous: true

must_haves:
  truths:
    - "RudderStack Node SDK is initialized in tracker with Kafka destination URL pointing to running Redpanda instance"
    - "Events from tracker queue are batched and sent to RudderStack Kafka destination"
    - "RudderStack destination produces events to Redpanda topic 'lead-events' matching Phase 1 setup"
    - "Verify via ClickHouse: manually captured events from tracker appear as rows in click_events table within 5 seconds"
  artifacts:
    - path: "src/tracker/rudderstack.js"
      provides: "RudderStack SDK initialization and event flushing"
      exports: ["initRudderStack", "trackEvent"]
    - path: "infra/rudderstack/workspaceConfig.json"
      provides: "RudderStack Kafka destination configuration pointing to Redpanda localhost:19092"
      contains: "destination.*Kafka"
    - path: "package.json"
      provides: "RudderStack SDK dependency (@rudderstack/sdk-js or @rudderstack/sdk-node)"
  key_links:
    - from: "src/tracker/index.js event queue"
      to: "src/tracker/rudderstack.js trackEvent()"
      via: "initRudderStack() called in tracker init"
      pattern: "import.*rudderstack|require.*rudderstack"
    - from: "src/tracker/rudderstack.js"
      to: "RudderStack HTTP endpoint (localhost:8080)"
      via: "SDK HTTP POST"
      pattern: "http://localhost:8080|rudderstack.*endpoint"
    - from: "RudderStack destination (infra config)"
      to: "Redpanda topic 'lead-events' (localhost:19092)"
      via: "Kafka destination configured in workspaceConfig.json"
      pattern: "kafkaConfig.*lead-events|destination.*kafka"
---

<objective>
Integrate RudderStack SDK with the tracker module, verify Kafka destination is correctly configured against Redpanda, and resolve the Phase 2 blocker by hands-on validation.

Purpose: Bridge tracker events to the event pipeline; confirm RudderStack destination fields are correct before full ingestion.
Output: Working RudderStack integration with Kafka destination validated against running Redpanda.
</objective>

<execution_context>
@/home/mootez/.claude/get-shit-done/workflows/execute-plan.md
@/home/mootez/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
Phase 1 summary: RudderStack running at localhost:8080, configured in infra/rudderstack/workspaceConfig.json with file-based backend config (deterministic, no control plane).

Blocker noted in STATE.md: "RudderStack self-hosted Kafka destination config against Redpanda needs hands-on spike before full tracker build — confirmed in docs but exact destination fields unverified"

This plan resolves that blocker in Task 1 (spike/verification) before implementing SDK integration in Task 2.

@/home/mootez/Desktop/pfa/.planning/phases/02-js-tracker-and-event-ingestion/02-01-SUMMARY.md (after 02-01 completes)
@/home/mootez/Desktop/pfa/infra/rudderstack/workspaceConfig.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Spike and verify RudderStack Kafka destination configuration against Redpanda</name>
  <files>infra/rudderstack/workspaceConfig.json, .env.example</files>
  <action>
    BLOCKER RESOLUTION TASK:
    
    1. Inspect current RudderStack config: cat infra/rudderstack/workspaceConfig.json
       - Identify if Kafka destination is already defined
       - If not, add a Kafka destination with these fields:
         * brokers: ["localhost:19092"] (Redpanda broker hostnames, verified against docker-compose)
         * topic: "lead-events" (must match Phase 1 setup)
         * auth: none (local setup, no SASL)
         
    2. Verify the config structure by:
       - Check RudderStack docs for self-hosted Kafka destination fields
       - Confirm field names (brokers, topic, auth) against actual RudderStack schema
       - If field names differ (e.g., bootstrap.servers instead of brokers), update accordingly
       
    3. Add .env variables if needed:
       - RUDDERSTACK_KAFKA_BROKERS=localhost:19092
       - RUDDERSTACK_KAFKA_TOPIC=lead-events
       
    4. Document exact destination config in task summary for future reference
       
    This task BLOCKS Task 2 - only proceed if destination config is verified correct.
  </action>
  <verify>
    - [ ] infra/rudderstack/workspaceConfig.json contains a Kafka destination section
    - [ ] Destination fields match RudderStack SDK documentation for self-hosted Kafka
    - [ ] Brokers point to "localhost:19092"
    - [ ] Topic is "lead-events"
    - [ ] RudderStack container is healthy and logs show no destination auth errors
  </verify>
  <done>RudderStack Kafka destination configuration verified and documented. Blocker resolved.</done>
</task>

<task type="auto">
  <name>Task 2: Add RudderStack SDK dependency and create SDK initialization module</name>
  <files>package.json, src/tracker/rudderstack.js</files>
  <action>
    1. Add RudderStack SDK to package.json:
       - Use @rudderstack/sdk-js (browser environment, no Node.js required yet)
       - Alternative: @rudderstack/sdk-node if Node backend desired (confirm with user)
       - Add to devDependencies: "npm install @rudderstack/sdk-js"
       
    2. Create src/tracker/rudderstack.js:
       - Export initRudderStack(writeKey, dataPlaneUrl):
         * writeKey: RudderStack source identifier (get from workspaceConfig.json)
         * dataPlaneUrl: "http://localhost:8080" (or from env)
         * Initialize SDK with load() or similar method
         * Return initialized instance
         
       - Export trackEvent(eventName, properties):
         * Call rudderstack.track(eventName, properties)
         * Automatically includes session_id, timestamp, device info
         
    3. RudderStack initialization options:
       - Data plane URL: http://localhost:8080
       - Write key: Extract from workspaceConfig.json (or use placeholder "test-key")
       - Enable debugging in dev: { debug: true } option
       
    NO external dependencies beyond RudderStack SDK.
  </action>
  <verify>
    - [ ] RudderStack SDK loads: npm list @rudderstack/sdk-js (or -node)
    - [ ] src/tracker/rudderstack.js exports initRudderStack and trackEvent
    - [ ] Can import and call: const { initRudderStack } = require('./src/tracker/rudderstack')
    - [ ] SDK initializes without errors
  </verify>
  <done>RudderStack SDK installed and initialization module created.</done>
</task>

<task type="auto">
  <name>Task 3: Wire tracker event queue to RudderStack and validate end-to-end to Redpanda</name>
  <files>src/tracker/index.js, src/tracker/rudderstack.js</files>
  <action>
    1. Update src/tracker/index.js:
       - Import { initRudderStack, trackEvent } from './rudderstack'
       - On module load: initRudderStack("test-write-key", "http://localhost:8080")
       - Add flushEvents() function that iterates event queue and calls trackEvent(event.type, event) for each
       - Call flushEvents() every 5 seconds (or on demand) to send queued events to RudderStack
       
    2. Create test validation:
       - Generate 5 test events manually (click, scroll, mousemove, pageview)
       - Call tracker.flushEvents()
       - Wait 5 seconds
       - Query ClickHouse: SELECT * FROM analytics.click_events WHERE event_type IN ('click', 'pageview') LIMIT 5
       - Verify rows exist with correct x_pct, y_pct, selector fields
       
    3. If events don't appear in ClickHouse:
       - Check RudderStack logs: docker compose logs rudderstack
       - Check Redpanda: rpk topic consume lead-events --from-beginning --max-messages 5
       - Check ClickHouse ingestion: SELECT * FROM analytics.events_queue LIMIT 5
       
    4. Commit verification: Create scripts/validate-rudderstack.sh that:
       - Publishes 3 test events
       - Waits 5 seconds
       - Queries ClickHouse for presence
       - Exits 0 if found, 1 if not
  </action>
  <verify>
    - [ ] tracker flushEvents() calls RudderStack SDK
    - [ ] RudderStack logs show events received (docker compose logs rudderstack | grep -i event)
    - [ ] Redpanda topic contains events: rpk topic consume lead-events
    - [ ] ClickHouse.click_events has rows from tracker: SELECT COUNT(*) FROM analytics.click_events WHERE event_type='click'
    - [ ] End-to-end latency <5 seconds (tracker → RudderStack → Redpanda → ClickHouse)
    - [ ] Coordinate data preserved: x_pct and y_pct values in ClickHouse match originals
  </verify>
  <done>RudderStack SDK wired to tracker; events flow through Redpanda to ClickHouse in <5 seconds. Blocker RESOLVED.</done>
</task>

</tasks>

<verification>
RudderStack Kafka destination is verified configured correctly by hands-on testing. Events flow from tracker → RudderStack → Redpanda → ClickHouse within 5 seconds. Blocker resolved and documented.
</verification>

<success_criteria>
- [ ] RudderStack Kafka destination fields verified correct in infra/rudderstack/workspaceConfig.json
- [ ] @rudderstack/sdk-js or -node installed and callable
- [ ] Tracker event queue flushes to RudderStack
- [ ] Events appear in Redpanda topic "lead-events"
- [ ] Events appear in ClickHouse click_events table with x_pct, y_pct preserved
- [ ] End-to-end latency <5 seconds
- [ ] No console or service errors
- [ ] Ready for cookie consent gating (Phase 2.03)
</success_criteria>

<output>
After completion, create `.planning/phases/02-js-tracker-and-event-ingestion/02-02-SUMMARY.md`
</output>
