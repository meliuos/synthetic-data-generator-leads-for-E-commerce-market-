---
phase: 02-js-tracker-and-event-ingestion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/tracker/index.js, src/tracker/events.js, src/tracker/coordinates.js, src/tracker/constants.js, package.json]
autonomous: true

must_haves:
  truths:
    - "Click events capture document-relative x_pct and y_pct coordinates (0-100% of viewport)"
    - "Scroll events record max scroll depth percentage for current page session"
    - "Mouse movement events are throttled to max 10 per second per session"
    - "Page view events emit on initial load with document dimensions"
    - "SPA route changes detected via History API interception emit new page_view events"
  artifacts:
    - path: "src/tracker/index.js"
      provides: "Main tracker entry point and initialization"
      exports: ["initTracker()"]
    - path: "src/tracker/events.js"
      provides: "Event capturing logic (click, scroll, mousemove, pageview)"
      exports: ["captureClick", "captureScroll", "captureMousemove", "capturePageView"]
    - path: "src/tracker/coordinates.js"
      provides: "Coordinate normalization to x_pct, y_pct (0-100 scale)"
      exports: ["normalizeCoordinates"]
    - path: "src/tracker/constants.js"
      provides: "Configuration constants (throttle intervals, selectors)"
      exports: ["EVENT_TYPES", "THROTTLE_INTERVALS"]
  key_links:
    - from: "window click event listener"
      to: "captureClick -> normalizeCoordinates -> event queue"
      via: "addEventListener on document"
      pattern: "document.addEventListener.*click"
    - from: "window scroll event listener"
      to: "captureScroll -> max depth tracking"
      via: "addEventListener on window"
      pattern: "window.addEventListener.*scroll"
    - from: "mousemove throttle"
      to: "event queue"
      via: "setInterval or throttle function"
      pattern: "setInterval|throttle"
    - from: "History API"
      to: "capturePageView"
      via: "pushState/replaceState interception"
      pattern: "history\\.(pushState|replaceState)"
---

<objective>
Create the core JavaScript tracker that captures clicks, scrolls, mouse movement, page views, and SPA route changes with document-relative coordinates normalized to percentages.

Purpose: Establish the foundation event capture mechanism before RudderStack integration and consent gating.
Output: Reusable tracker module (src/tracker/) with 5 event types and coordinate normalization.
</objective>

<execution_context>
@/home/mootez/.claude/get-shit-done/workflows/execute-plan.md
@/home/mootez/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
Phase 1 complete: Redpanda + ClickHouse + RudderStack stack running locally with smoke test passing.

@/home/mootez/Desktop/pfa/.planning/PROJECT.md
@/home/mootez/Desktop/pfa/.planning/REQUIREMENTS.md
@/home/mootez/Desktop/pfa/.planning/phases/01-streaming-and-storage-backbone/01-01-SUMMARY.md
@/home/mootez/Desktop/pfa/.planning/phases/01-streaming-and-storage-backbone/01-03-SUMMARY.md

Key decision from Phase 2 REQUIREMENTS:
- Click events must use document-relative coordinates (x_pct, y_pct as % of full document), not viewport-relative
- This normalization cannot be changed after data flows or historical data is unrecoverable
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create tracker module scaffolding and constants</name>
  <files>src/tracker/index.js, src/tracker/constants.js, package.json</files>
  <action>
    Create src/tracker/ directory structure with:
    
    1. src/tracker/constants.js: Define EVENT_TYPES = {CLICK, SCROLL, MOUSEMOVE, PAGE_VIEW}; THROTTLE_INTERVALS (100ms for mousemove, 10/sec = ~100ms);
    
    2. src/tracker/index.js: Export initTracker() function that:
       - Takes optional config (consent gate flag, event queue reference)
       - Attaches all event listeners when called
       - Exports getSessionId() for session tracking
       
    3. package.json: Add placeholder project metadata (name: "lead-tracker", version: "0.1.0")
    
    Skeleton only - event handlers will be implemented in Task 2-3.
  </action>
  <verify>
    - [ ] src/tracker/index.js exists
    - [ ] src/tracker/constants.js exists
    - [ ] imports work: const { initTracker } = require('./src/tracker')
  </verify>
  <done>Tracker directory structure in place with constants and init placeholder.</done>
</task>

<task type="auto">
  <name>Task 2: Implement click and page view event capture with coordinate normalization</name>
  <files>src/tracker/events.js, src/tracker/coordinates.js, src/tracker/index.js</files>
  <action>
    1. src/tracker/coordinates.js: Implement normalizeCoordinates(clientX, clientY):
       - Calculate x_pct = (clientX / document.documentElement.scrollWidth) * 100
       - Calculate y_pct = (clientY / document.documentElement.scrollHeight) * 100 (from top of document including scroll offset)
       - Return { x_pct: rounded to 1 decimal, y_pct: rounded to 1 decimal }
       - Test case: Click at middle of 800x600 viewport should be ~50%, 50%
       
    2. src/tracker/events.js: Implement:
       - captureClick(event): Extract clientX/clientY, normalize to x_pct/y_pct, get element selector via event.target.tagName + classList, emit {type: CLICK, x_pct, y_pct, selector, timestamp}
       - capturePageView(): Get url, title, document dimensions, emit {type: PAGE_VIEW, url, title, width: window.innerWidth, height: window.innerHeight, timestamp}
       
    3. Update src/tracker/index.js:
       - In initTracker(), attach document.addEventListener('click', captureClick)
       - In initTracker(), call capturePageView() on page load
       - Store events in module-level queue array (will be flushed by RudderStack in Phase 2.02)
       
    NO EXTERNAL DEPENDENCIES - vanilla JS only at this phase.
  </action>
  <verify>
    - [ ] Manually test: Click on page element, console.log tracker event queue → shows click event with x_pct/y_pct
    - [ ] test: Page load → event queue contains PAGE_VIEW event with url and dimensions
    - [ ] Test coordinate normalization: Click at (0,0) should be close to (0,0)%; click at (scrollWidth, scrollHeight) should be close to (100, 100)%
  </verify>
  <done>Click and page view events captured with document-relative coordinates (x_pct, y_pct). Event queue populated.</done>
</task>

<task type="auto">
  <name>Task 3: Implement scroll depth and mousemove event capture with throttling</name>
  <files>src/tracker/events.js, src/tracker/index.js</files>
  <action>
    1. src/tracker/events.js: Implement:
       - captureScroll(event): Calculate scroll_pct = (window.scrollY / (document.documentElement.scrollHeight - window.innerHeight)) * 100, store max_scroll_pct per session, emit {type: SCROLL, scroll_pct, timestamp}
       - captureMousemove(event): Throttled version - only fire if last mousemove was >100ms ago (10/sec max). Call normalizeCoordinates for mousemove event, emit {type: MOUSEMOVE, x_pct, y_pct, timestamp}
       
    2. Throttle implementation (inline or simple helper): 
       - Use timestamp check: const lastMousemoveTime = 0; if (now - lastMousemoveTime > 100ms) { capture and update lastMousemoveTime }
       
    3. Update src/tracker/index.js:
       - In initTracker(), attach window.addEventListener('scroll', captureScroll)
       - In initTracker(), attach document.addEventListener('mousemove', captureMousemove)
       - Both should push to module-level event queue
       
    CRITICAL: Mousemove must not overwhelm queue - enforce 100ms throttle strictly.
  </action>
  <verify>
    - [ ] Scroll page 50% down → event queue contains SCROLL event with scroll_pct ~= 50
    - [ ] Scroll 100% to bottom → max_scroll_pct is recorded
    - [ ] Rapidly move mouse for 1 second → event queue has no more than ~10 MOUSEMOVE events (10/sec throttle)
    - [ ] Run for 10 seconds: queue size grows linearly with time (not exponentially), proving throttle works
  </verify>
  <done>Scroll depth and throttled mousemove events captured. Event queue ready for RudderStack integration.</done>
</task>

</tasks>

<verification>
All event types (click, scroll, mousemove, page_view) are captured with correct document-relative coordinates and stored in module-level queue. Throttling prevents mousemove flooding. Next plan (02-02) will wire RudderStack SDK to flush this queue.
</verification>

<success_criteria>
- [ ] src/tracker/ module loads without errors
- [ ] initTracker() executes on page load
- [ ] Click events appear in queue with x_pct, y_pct (0-100 range)
- [ ] Scroll events track max scroll depth percentage
- [ ] Mousemove events are throttled: <10 per second
- [ ] Page view events emit on load and SPA navigation
- [ ] No console errors
- [ ] Ready for RudderStack SDK integration
</success_criteria>

<output>
After completion, create `.planning/phases/02-js-tracker-and-event-ingestion/02-01-SUMMARY.md`
</output>
