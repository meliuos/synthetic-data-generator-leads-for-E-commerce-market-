---
phase: 8
plan: 1
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - dashboard/heatmap_queries.py
  - dashboard/app.py
  - dashboard/tests/test_phase8_queries.py
autonomous: true

must_haves:
  truths:
    - "Session stats panel computes total sessions, avg scroll depth, bounce rate, and total events in ClickHouse SQL"
    - "Click ranking panel computes top 10 selectors for click events in ClickHouse SQL"
    - "Both queries reuse the existing URL scoping helper semantics"
    - "Dashboard renders graceful empty states when scoped data is absent"
    - "No raw event rows are fetched into Python for these two panels"
  artifacts:
    - path: "dashboard/heatmap_queries.py"
      provides: "Aggregated ClickHouse query helpers for STATS-01 and STATS-02"
      contains: "GROUP BY"
    - path: "dashboard/app.py"
      provides: "Streamlit panels for session stats and click ranking"
      contains: "No sessions yet"
      contains_alt: "No clicks yet"
    - path: "dashboard/tests/test_phase8_queries.py"
      provides: "Unit tests for SQL shape and URL scope parameterization"
      contains: "build_session_stats_query"
      contains_alt: "build_click_ranking_query"
---

<objective>
Implement Phase 8 in one plan by adding two dashboard panels to the existing Streamlit app and adding query helpers in dashboard/heatmap_queries.py that aggregate in ClickHouse: session stats (STATS-01) and click ranking (STATS-02). Ensure URL scoping semantics exactly match existing heatmap behavior (exact URL and wildcard with * mapped to LIKE) and empty states render gracefully.

Output: query helpers, UI integration, and tests for SQL/parameter behavior.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@dashboard/heatmap_filters.py
@dashboard/heatmap_queries.py
@dashboard/app.py
@infra/clickhouse/sql/001_events_schema.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Phase 8 query helpers in heatmap_queries.py</name>
  <files>dashboard/heatmap_queries.py</files>
  <action>
Add two helper query builders plus fetch wrappers:
- build_session_stats_query(url_filter)
- fetch_session_stats(client, url_filter)
- build_click_ranking_query(url_filter, limit=10)
- fetch_click_ranking(client, url_filter, limit=10)

Rules:
- Reuse build_url_predicate(url_filter) for scope.
- Session stats SQL must aggregate in ClickHouse and return one row with columns:
  total_sessions, avg_scroll_depth_pct, bounce_rate_pct, total_events
- Bounce rate definition: sessions with exactly one page_view event / total sessions.
- Avg scroll depth should be based on per-session max scroll_pct, averaged across sessions.
- Click ranking SQL returns up to 10 rows: element_selector, click_count ordered desc.
- Do not fetch raw rows and compute metrics in Python.
  </action>
  <verify>
python3 -m unittest dashboard.tests.test_phase8_queries -v
  </verify>
  <done>
heatmap_queries.py exposes Phase 8 query helpers that aggregate session stats and click ranking in SQL using existing URL scope semantics.
  </done>
</task>

<task type="auto">
  <name>Task 2: Integrate session stats and click ranking panels in dashboard app</name>
  <files>dashboard/app.py</files>
  <action>
Integrate new panels into dashboard/app.py:
- Session stats panel with four metrics and tooltip for bounce-rate definition.
- Click ranking panel table (selector + clicks) showing top 10.
- Both panels rerun with the active URL scope.
- Graceful empty states: "No sessions yet" and "No clicks yet".

Keep existing heatmap tabs behavior unchanged.
  </action>
  <verify>
python3 -m unittest dashboard.tests.test_phase8_queries -v
  </verify>
  <done>
Dashboard shows two new Phase 8 panels that read aggregated data from ClickHouse and degrade gracefully for empty scopes.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add focused unit tests for query shape and scope semantics</name>
  <files>dashboard/tests/test_phase8_queries.py</files>
  <action>
Create tests validating:
- URL scoping: exact URL -> page_url = parameter, wildcard -> LIKE parameter.
- Session stats SQL includes GROUP BY / countIf / avg and does not expose raw event row projection.
- Click ranking SQL filters event_type='click', groups by element_selector, orders by click_count desc, and limits to 10 by default.
- Parameter dictionaries include expected keys and values.
  </action>
  <verify>
python3 -m unittest dashboard.tests.test_phase8_queries -v
  </verify>
  <done>
Tests pass and lock Phase 8 SQL behavior to ClickHouse-side aggregation and existing URL filtering semantics.
  </done>
</task>

</tasks>

<verification>
# Tests
python3 -m unittest dashboard.tests.test_phase8_queries -v

# Query helper surface exists
rg -n "build_session_stats_query|fetch_session_stats|build_click_ranking_query|fetch_click_ranking" dashboard/heatmap_queries.py

# Empty-state text exists in app
rg -n "No sessions yet|No clicks yet|bounce" dashboard/app.py
</verification>

<success_criteria>
- [ ] STATS-01 implemented with four metrics computed in ClickHouse SQL
- [ ] STATS-02 implemented with top-10 selector ranking computed in ClickHouse SQL
- [ ] URL scope semantics match existing helper behavior
- [ ] Graceful empty states render for zero rows
- [ ] Unit tests added and passing
</success_criteria>
