---
phase: 04-heatmap-computation-and-core-dashboard
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - dashboard/app.py
  - dashboard/heatmap_plotly.py
  - dashboard/requirements.txt
autonomous: true
must_haves:
  truths:
    - "Selecting a page URL renders a heatmap over the matching screenshot"
    - "The click view is drawn as a Plotly overlay on top of the screenshot image"
    - "The dashboard loads screenshots from the predictable local path created in Phase 3"
  artifacts:
    - path: "dashboard/app.py"
      provides: "Streamlit app with screenshot-backed click heatmap view"
      min_lines: 180
      contains:
        - "st.plotly_chart"
        - "st.tabs"
        - "capture_screenshot"
    - path: "dashboard/heatmap_plotly.py"
      provides: "Plotly figure builder for screenshot-backed heatmap overlays"
      min_lines: 80
      contains:
        - "go.Heatmap"
        - "layout.images"
    - path: "dashboard/requirements.txt"
      provides: "Dashboard dependencies for Plotly rendering"
      contains:
        - "plotly"
  key_links:
    - from: "dashboard/app.py"
      to: "dashboard/heatmap_plotly.py"
      via: "figure builder import"
      pattern: "heatmap_plotly"
    - from: "dashboard/app.py"
      to: "./screenshots/{hash}/{viewport}.png"
      via: "local screenshot path lookup"
      pattern: "screenshots|sha256"
    - from: "dashboard/heatmap_plotly.py"
      to: "Plotly layout.images"
      via: "screenshot background layer"
      pattern: "layout\.images"
---

<objective>
Turn the Streamlit dashboard into a screenshot-backed click heatmap viewer. The existing screenshot flow stays intact, but the tab content should now render a Plotly overlay rather than a plain image when click heatmap data is available.

Purpose: This is the first user-visible Phase 4 deliverable and the foundation for the other heatmap modes.

Output: Dashboard renders click heatmaps over the correct page screenshot using Plotly and the data returned by the new query layer.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/research/STACK.md
@.planning/research/ARCHITECTURE.md
@.planning/research/SUMMARY.md
@.planning/phases/03-screenshot-capture-service/03-02-SUMMARY.md
@.planning/phases/04-heatmap-computation-and-core-dashboard/04-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Create Plotly heatmap overlay builder</name>
  <files>dashboard/heatmap_plotly.py</files>
  <action>
Create a small rendering helper that turns the ClickHouse aggregate dataframe into a Plotly figure layered over a screenshot image.

Implement the builder so it:
- Accepts the screenshot path, the binned dataframe, and the viewport dimensions.
- Uses `go.Heatmap` for the density layer and `layout.images` for the screenshot background.
- Anchors the overlay to the screenshot dimensions so the bins line up with the pixels from Phase 3.
- Keeps the figure ready for `st.plotly_chart()` without extra dashboard-side manipulation.

Avoid introducing any browser-side canvas code. Plotly should remain the rendering surface.
  </action>
  <verify>
- `python -m py_compile dashboard/heatmap_plotly.py` succeeds
- A minimal local script can instantiate a figure without raising Plotly errors
- The helper exposes one clear entry point for the dashboard to call
  </verify>
  <done>
- Plotly figure construction is isolated in a reusable helper
- Screenshot and heatmap layers are aligned in one overlay figure
  </done>
</task>

<task type="auto">
  <name>Refactor the Streamlit app to render click heatmaps on screenshots</name>
  <files>dashboard/app.py</files>
  <action>
Refactor the dashboard so the screenshot section becomes a click heatmap viewer rather than an image-only viewer.

Keep the existing screenshot refresh workflow, but change the rendering path to:
- load the selected screenshot from the predictable Phase 3 filesystem path,
- fetch click heatmap aggregates through the Phase 4 query helper,
- pass the screenshot path and aggregate dataframe into the Plotly builder,
- render the result in the desktop/mobile tabs with `st.plotly_chart`.

Preserve the current URL selection and refresh controls. The new work should extend that workflow, not replace it.
  </action>
  <verify>
- `streamlit run dashboard/app.py` starts without import errors
- Clicking a selected page renders a Plotly heatmap overlay on the screenshot
- The dashboard still refreshes screenshots on demand
- No raw event rows are fetched directly into the UI layer
  </verify>
  <done>
- The dashboard shows a click heatmap overlay for the selected URL
- Screenshot refresh remains functional
- Plotly rendering replaces the old static image display in the click view
  </done>
</task>

<task type="auto">
  <name>Add Plotly to the dashboard dependencies</name>
  <files>dashboard/requirements.txt</files>
  <action>
Add the Plotly package version required by the new overlay builder. Keep the dependency list clean and avoid unrelated pin churn.
  </action>
  <verify>
- `grep "plotly" dashboard/requirements.txt` returns the new package line
- The dashboard environment can import Plotly after dependency installation
  </verify>
  <done>
- Plotly is available to Streamlit for screenshot-backed heatmap rendering
  </done>
</task>

</tasks>

<verification>
Start the Streamlit dashboard and confirm the selected page renders a screenshot-backed click heatmap overlay. The result should still honor the existing screenshot refresh controls and should not expose raw events in the UI.
</verification>

<success_criteria>
- Click heatmaps render as Plotly overlays on top of the correct screenshot
- The dashboard continues to use the Phase 3 screenshot store
- Plotly dependencies are installed and importable
- UI output remains screenshot-driven, not a raw data table
</success_criteria>

<output>
After completion, create `.planning/phases/04-heatmap-computation-and-core-dashboard/04-02-SUMMARY.md`
</output>
