---
phase: 04-heatmap-computation-and-core-dashboard
plan: 04
type: execute
wave: 4
depends_on:
  - 04-03
files_modified:
  - dashboard/app.py
  - dashboard/heatmap_views.py
autonomous: true
must_haves:
  truths:
    - "Scroll depth mode renders horizontal bands showing max scroll depth distribution"
    - "Hover mode renders mousemove density on the same screenshot canvas"
    - "Both additional modes respect the URL filter and viewport selection already in place"
  artifacts:
    - path: "dashboard/app.py"
      provides: "Final Streamlit dashboard with scroll and hover heatmap rendering"
      min_lines: 260
      contains:
        - "scroll"
        - "mousemove"
        - "st.plotly_chart"
    - path: "dashboard/heatmap_views.py"
      provides: "Mode-specific aggregation and overlay helpers for scroll and hover views"
      min_lines: 100
      contains:
        - "scroll"
        - "mousemove"
        - "heatmap"
  key_links:
    - from: "dashboard/heatmap_views.py"
      to: "dashboard/heatmap_queries.py"
      via: "mode-specific query reuse"
      pattern: "scroll|mousemove"
    - from: "dashboard/app.py"
      to: "dashboard/heatmap_views.py"
      via: "view-mode dispatch"
      pattern: "heatmap_views"
---

<objective>
Finish Phase 4 by adding the remaining scroll depth and hover/movement heatmap views to the already filtered, screenshot-backed dashboard.

Purpose: The phase is only complete when the app can switch between all three visual modes with the correct semantics, not just click heatmaps.

Output: A single Streamlit dashboard that renders click, scroll, and hover heatmaps over screenshots using ClickHouse aggregates and the shared filter controls.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/research/STACK.md
@.planning/research/ARCHITECTURE.md
@.planning/research/SUMMARY.md
@.planning/phases/04-heatmap-computation-and-core-dashboard/04-02-PLAN.md
@.planning/phases/04-heatmap-computation-and-core-dashboard/04-03-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Implement scroll and hover aggregation helpers</name>
  <files>dashboard/heatmap_views.py</files>
  <action>
Add mode-specific helpers for the remaining two Phase 4 views.

Implement logic that:
- Builds scroll-depth aggregates as horizontal bands using the same 5% bucketed event data.
- Builds hover / mousemove aggregates with the same grid semantics as click heatmaps.
- Reuses the existing ClickHouse query contract instead of introducing a second data path.
- Returns data that the Plotly overlay builder can consume without additional transformation in the UI.

Keep the helpers separate from the main app so the mode-specific math stays testable and easy to read.
  </action>
  <verify>
- `python -m py_compile dashboard/heatmap_views.py` succeeds
- The helper functions expose scroll and mousemove aggregate shapes the dashboard can render directly
  </verify>
  <done>
- Scroll and hover aggregation logic exists outside the main app file
- Both modes produce heatmap-ready data for the overlay renderer
  </done>
</task>

<task type="auto">
  <name>Wire scroll depth and hover heatmap rendering into the dashboard</name>
  <files>dashboard/app.py</files>
  <action>
Extend the existing Streamlit render path so the dashboard can switch to the remaining two heatmap modes.

Update the app so it:
- dispatches scroll mode to the horizontal band rendering path,
- dispatches hover mode to the mousemove density path,
- keeps the screenshot overlay aligned with the Phase 3 image dimensions,
- shows empty states when a page has no scroll or hover events yet,
- preserves the filter and screenshot controls already added in earlier plans.

Do not duplicate the click view layout. Extend the same dashboard frame with mode-specific render logic.
  </action>
  <verify>
- `streamlit run dashboard/app.py` shows scroll and hover modes in the mode switcher
- Scroll mode renders horizontal gradient bands on the screenshot
- Hover mode renders mousemove density on the screenshot
- URL filters still apply in both views
  </verify>
  <done>
- The dashboard completes Phase 4 visual coverage with scroll and hover views
- All three heatmap modes render from the same screenshot-driven dashboard shell
  </done>
</task>

</tasks>

<verification>
Run the app and toggle through click, scroll, and hover modes. Confirm each mode renders from ClickHouse aggregates on top of the screenshot and that the URL filter continues to apply across all views.
</verification>

<success_criteria>
- Scroll depth heatmaps render as horizontal bands on the screenshot
- Hover / movement heatmaps render with the same grid semantics as other modes
- Mode switching works without breaking the screenshot viewer or URL filter
- The dashboard still avoids raw event fetches in the UI process
</success_criteria>

<output>
After completion, create `.planning/phases/04-heatmap-computation-and-core-dashboard/04-04-SUMMARY.md`
</output>
