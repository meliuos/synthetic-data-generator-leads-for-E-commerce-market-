---
phase: 04-heatmap-computation-and-core-dashboard
plan: 03
type: execute
wave: 3
depends_on:
  - 04-02
files_modified:
  - dashboard/app.py
  - dashboard/heatmap_filters.py
autonomous: true
must_haves:
  truths:
    - "Users can filter the dashboard by exact page URL or wildcard pattern such as /product/*"
    - "Changing the filter updates all heatmap queries consistently"
    - "Users can switch the dashboard between click, scroll, and hover modes from one control surface"
  artifacts:
    - path: "dashboard/app.py"
      provides: "Dashboard controls for URL filtering and heatmap type switching"
      min_lines: 220
      contains:
        - "st.selectbox"
        - "st.text_input"
        - "heatmap mode"
    - path: "dashboard/heatmap_filters.py"
      provides: "URL filter normalization and wildcard-to-predicate conversion"
      min_lines: 80
      contains:
        - "*"
        - "LIKE"
  key_links:
    - from: "dashboard/app.py"
      to: "dashboard/heatmap_filters.py"
      via: "filter normalization import"
      pattern: "heatmap_filters"
    - from: "dashboard/app.py"
      to: "dashboard/heatmap_queries.py"
      via: "shared query parameterization"
      pattern: "event_type|page_url"
---

<objective>
Add the dashboard controls that let users scope the heatmap to one page or a family of pages and switch the visual between click, scroll, and hover modes without changing the rest of the layout.

Purpose: Phase 4 needs usable controls before the additional heatmap modes can be meaningful. This plan makes the app navigable and keeps the query layer consistent across modes.

Output: Dashboard supports exact URL selection, wildcard URL patterns, and a single heatmap mode switcher that drives the existing Plotly overlay path.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/research/STACK.md
@.planning/research/ARCHITECTURE.md
@.planning/research/SUMMARY.md
@.planning/phases/04-heatmap-computation-and-core-dashboard/04-01-PLAN.md
@.planning/phases/04-heatmap-computation-and-core-dashboard/04-02-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Build URL filter normalization helpers</name>
  <files>dashboard/heatmap_filters.py</files>
  <action>
Create a focused helper module that normalizes page URL inputs for the dashboard.

Implement support for:
- Exact URL selection from known pages.
- Wildcard inputs like `/product/*` or `/category/*`.
- Translation of wildcard syntax into the predicate format used by the ClickHouse query layer.
- Reuse of the same normalized filter across every heatmap mode.

Keep the helper deterministic and small. The dashboard should not need to duplicate wildcard parsing or path matching logic.
  </action>
  <verify>
- `python -m py_compile dashboard/heatmap_filters.py` succeeds
- The helper converts wildcard text into the expected predicate or parameter structure
  </verify>
  <done>
- URL filter normalization is isolated from the Streamlit UI code
- Exact and wildcard page scopes share the same contract
  </done>
</task>

<task type="auto">
  <name>Add URL filter and heatmap mode controls to the dashboard</name>
  <files>dashboard/app.py</files>
  <action>
Wire the dashboard controls so users can choose the page scope and the active heatmap mode from one interface.

Update the app so it:
- exposes exact URL selection plus a wildcard text input,
- uses the normalized filter for every query path,
- provides a click / scroll / hover mode switcher,
- keeps the screenshot viewer and Plotly overlay layout intact,
- routes the selected mode to the shared rendering path instead of duplicating UI blocks.

The goal is to centralize control state before the scroll and hover view work lands.
  </action>
  <verify>
- `streamlit run dashboard/app.py` shows the URL filter controls and mode switcher
- Entering `/product/*` updates the active dataset to matching URLs
- Switching modes preserves the selected page scope and screenshot
  </verify>
  <done>
- Dashboard has a single control surface for page scope and heatmap mode
- Wildcard filtering works consistently across the active views
  </done>
</task>

</tasks>

<verification>
Run the app and confirm the selected filter and mode propagate through the existing click heatmap path. Wildcard inputs should expand the result set instead of behaving like literal URLs.
</verification>

<success_criteria>
- Users can scope the dashboard to one URL or a family of URLs with wildcard syntax
- The dashboard can switch heatmap type from one control surface
- The active filter is reused across all heatmap queries and visual states
- The click view still functions after the control refactor
</success_criteria>

<output>
After completion, create `.planning/phases/04-heatmap-computation-and-core-dashboard/04-03-SUMMARY.md`
</output>
